// 2nd Question
module.exports = function triangle(string) {
    temp_str = "";
    for (let i of string) {
        temp_str = temp_str + i;
        console.log(temp_str)
    }
}

// No output as this is just used in the import